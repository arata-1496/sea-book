"use client";
import { AnimalData } from "@/data/animalData";

export const setAnimals = () => {
  const getRandamNumber = () => {
    const randamNum = Math.floor(Math.random() * 3) + 1;
  };

  return <div>setAnimals</div>;
};
